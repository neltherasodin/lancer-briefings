version: '3.8'
services:
  lancer-briefings:
    image: node:lts-alpine
    working_dir: /app
    volumes:
      - ./:/app
    ports:
      - "8013:5173" 
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
    restart: unless-stopped